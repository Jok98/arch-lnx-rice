# Shared Zsh palette so prompts and plugins reuse the same colors.
typeset -g ZSH_THEME_BASE_BG='#1a2332'       # deep backdrop (panels/backgrounds)
typeset -g ZSH_THEME_PRIMARY_FG='#ffc0a4'    # warm peach text
typeset -g ZSH_THEME_ACCENT='#e5a4ff'        # violet accent for highlights
